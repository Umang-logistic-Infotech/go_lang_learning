<!DOCTYPE html>
<html>
<head>
    <title>{{.PageTitle}}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 50px;
            background-color: #f5f5f5;
        }
        .back-link {
            display: inline-block;
            margin-bottom: 20px;
            color: #2196F3;
            text-decoration: none;
        }
        .back-link:hover {
            text-decoration: underline;
        }
        .profile-card {
            background: white;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
            max-width: 600px;
        }
        .profile-header {
            border-bottom: 2px solid #4CAF50;
            padding-bottom: 15px;
            margin-bottom: 20px;
        }
        .profile-header h1 {
            margin: 0;
            color: #333;
        }
        .badge {
            padding: 5px 15px;
            border-radius: 3px;
            font-size: 14px;
            font-weight: bold;
            margin-top: 10px;
            display: inline-block;
        }
        .badge-active {
            background-color: #4CAF50;
            color: white;
        }
        .badge-inactive {
            background-color: #f44336;
            color: white;
        }
        .info-row {
            display: flex;
            padding: 15px 0;
            border-bottom: 1px solid #eee;
        }
        .info-label {
            font-weight: bold;
            color: #666;
            width: 150px;
        }
        .info-value {
            color: #333;
        }
        .actions {
            margin-top: 30px;
        }
        .btn {
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 4px;
            display: inline-block;
            margin-right: 10px;
        }
        .btn-edit {
            background-color: #FF9800;
            color: white;
        }
    </style>
</head>
<body>
    <a href="/users" class="back-link">← Back to Users List</a>
    
    <div class="profile-card">
        <div class="profile-header">
            <h1>{{.User.Name}}</h1>
            {{if .User.IsActive}}
                <span class="badge badge-active">Active Account</span>
            {{else}}
                <span class="badge badge-inactive">Inactive Account</span>
            {{end}}
        </div>
        
        <div class="info-row">
            <div class="info-label">User ID:</div>
            <div class="info-value">{{.User.ID}}</div>
        </div>
        
        <div class="info-row">
            <div class="info-label">Email:</div>
            <div class="info-value">{{.User.Email}}</div>
        </div>
        
        <div class="info-row">
            <div class="info-label">Age:</div>
            <div class="info-value">
                {{if .User.Age}}
                    {{.User.Age}} years old
                {{else}}
                    Not specified
                {{end}}
            </div>
        </div>
        
        <div class="info-row">
            <div class="info-label">City:</div>
            <div class="info-value">
                {{if .User.City}}
                    {{.User.City}}
                {{else}}
                    Not specified
                {{end}}
            </div>
        </div>
        
        <div class="info-row">
            <div class="info-label">Member Since:</div>
        </div>
        
        <div class="info-row">
            <div class="info-label">Last Updated:</div>
        </div>
        
        <div class="actions">
            <a href="/users/{{.User.ID}}/edit" class="btn btn-edit">Edit Profile</a>
        </div>
    </div>
</body>
</html>